import React from 'react'
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementsByTagName('html'));
root.render(
    <React.StrictMode>
        <h1>
            Home
        </h1>
    </React.StrictMode>
);